# sample-label

## Purpose

The test print during Bluetooth setup, so the vendor checks the same label
an order will produce.

## Contents

- `route.ts` — `GET ?location=<id>`. Session-authenticated; renders a
  sample at the booth printer's own size and dpi.

## Parent

[bridge](../README.md)
